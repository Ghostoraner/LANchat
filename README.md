# LANChat

**LANChat** — кроссплатформенное приложение для обмена сообщениями в локальной сети (LAN).

Проект написан на **C# / .NET 10** с использованием **Avalonia UI** и использует архитектуру **Client–Server**. Обмен данными происходит через TCP-соединение с использованием собственного легковесного JSON-протокола.

> 🚧 Проект находится в разработке.

## ✨ Возможности

* 💬 Обмен сообщениями в реальном времени
* 🌐 Работа внутри локальной сети (LAN)
* 🖥️ Кроссплатформенность

  * Linux
  * Windows
* 👤 Никнеймы пользователей
* 🔔 Системные уведомления о подключении и отключении пользователей
* 📡 TCP-соединение между клиентом и сервером
* 📦 JSON-протокол для обмена данными
* 🎨 Графический интерфейс на Avalonia UI
* ⚡ Лёгкий сервер без сторонних backend-сервисов

## 🛠️ Технологии

| Технология         | Использование      |
| ------------------ | ------------------ |
| **C#**             | Основной язык      |
| **.NET 10**        | Платформа          |
| **Avalonia UI 11** | GUI клиента        |
| **TCP/IP**         | Сетевое соединение |
| **JSON**           | Формат сообщений   |

## 📁 Структура проекта

```text
LANChat/
├── LANChat.Client/
│   ├── Views/
│   ├── ViewModels/
│   ├── Models/
│   └── ...
│
├── LANChat.Server/
│   ├── Models/
│   ├── Services/
│   └── ...
│
├── LANChat.slnx
└── README.md
```

### `LANChat.Server`

Консольный TCP-сервер, который:

* принимает подключения клиентов;
* регистрирует пользователей;
* отслеживает подключения и отключения;
* принимает сообщения;
* рассылает сообщения подключённым клиентам.

### `LANChat.Client`

GUI-клиент на **Avalonia UI**, который позволяет:

* подключаться к серверу;
* указывать IP и порт;
* задавать никнейм;
* отправлять сообщения;
* получать сообщения от других пользователей;
* отображать системные события.

## ⚙️ Требования

Перед запуском убедитесь, что установлен:

* **.NET 10 SDK**
* GUI-окружение на Linux (X11 или Wayland)

Проверить установленную версию .NET:

```bash
dotnet --version
```

## 🚀 Запуск

### 1. Клонирование репозитория

```bash
git clone  https://github.com/Ghostoraner/LANchat.git
cd LANChat
```

### 2. Запуск сервера

Откройте терминал и выполните:

```bash
dotnet run --project LANChat.Server
```

По умолчанию сервер использует порт:

```text
5000
```

### 3. Запуск клиента

В другом терминале:

```bash
dotnet run --project LANChat.Client
```

### 4. Подключение

В клиенте укажите:

| Поле        | Значение                       |
| ----------- | ------------------------------ |
| **IP**      | IP-адрес компьютера с сервером |
| **Порт**    | `5000`                         |
| **Никнейм** | Любое имя                      |

Для запуска клиента и сервера на одном компьютере можно использовать:

```text
127.0.0.1
```

Для подключения с другого компьютера в той же сети необходимо указать **локальный IP компьютера, на котором запущен сервер**.

> ⚠️ Если компьютеры находятся в одной сети, но подключение не работает, проверьте настройки firewall и доступность порта `5000`.

## 📡 Протокол

LANChat использует собственный простой протокол поверх **TCP/IP**.

Данные передаются в формате **JSON**.

Пример сообщения:

```json
{
  "type": "message",
  "sender": "User",
  "content": "Привет всем!",
  "timestamp": "2026-09-05T19:00:00Z"
}
```

### Типы сообщений

| Тип       | Назначение                                    |
| --------- | --------------------------------------------- |
| `join`    | Подключение пользователя и установка никнейма |
| `message` | Обычное сообщение                             |
| `leave`   | Отключение пользователя                       |
| `system`  | Системное уведомление                         |

## 🔄 Как это работает

```text
┌──────────────┐
│   Client 1   │
└──────┬───────┘
       │
       │ TCP
       ▼
┌──────────────────┐
│   LANChat.Server │
└──────────────────┘
       ▲       ▲
       │       │
     TCP       TCP
       │       │
┌──────┴───┐ ┌─┴──────────┐
│ Client 2 │ │  Client 3  │
└──────────┘ └─────────────┘
```

Сервер является центральной точкой соединения:

1. Клиент подключается к серверу.
2. Клиент отправляет свой никнейм.
3. Сервер регистрирует пользователя.
4. Пользователь отправляет сообщение.
5. Сервер получает сообщение.
6. Сервер рассылает его другим подключённым клиентам.

## 🔐 Безопасность

На данный момент LANChat предназначен прежде всего для использования в доверенной локальной сети.

Протокол **не следует считать защищённым от прослушивания**, если соединение не дополнено шифрованием.

В будущих версиях планируется добавить дополнительные механизмы безопасности.

## 🗺️ Roadmap

* [x] TCP-сервер
* [x] GUI-клиент
* [x] Подключение пользователей
* [x] Валидация и защита от дублирования никнеймов
* [x] Обмен сообщениями
* [x] Системные уведомления
* [x] JSON-протокол и построчная потоковая передача
* [x] Список подключённых пользователей
* [ ] Защита от мертвых соединений (Keep-Alive таймауты)
* [ ] Защита от переполнения памяти (ограничение максимальной длины входящих строк)
* [ ] Ограничение частоты запросов (Rate Limiting / защита от спама)
* [ ] Изоляция ошибок при широковещательной рассылке (try-catch для каждого сокета в Broadcast)
* [ ] Шифрование сообщений (TLS / AES)
* [ ] Автоматическое обнаружение сервера в LAN
* [ ] История сообщений
* [ ] Передача файлов
* [ ] Улучшение UI/UX
* [ ] Готовые релизы для Linux и Windows

