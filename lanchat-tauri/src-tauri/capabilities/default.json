{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window — grants core APIs including the event system (listen/emit) used for incoming chat messages.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}